# Interactive FreeSurfer (Jupyter)
ARG DOCKER_BASE=common/freesurfer_base.Dockerfile
FROM ${DOCKER_BASE} as service-base

# Add Jupyter/Interactive layers here if needed (inherited from base image mostly)
# Ensure entrypoint is compatible with Jupyter (if distinct from base)

ENV SC_USER_ID=8004
ENV SC_USER_NAME=scu
# (Assuming base image has Jupyter or we stick to itisfoundation/jupyter-math behavior)

# ... specific additions ...
