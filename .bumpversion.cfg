[bumpversion]
current_version = 1.3.4
commit = False
message = service version: {current_version} → {new_version}
tag = False

[bumpversion:file:.osparc/runner-medimproc/metadata.yml]
search = {current_version}
replace = {new_version}

[bumpversion:file:.osparc/runner-medimproc-slim/metadata.yml]
search = {current_version}
replace = {new_version}

[bumpversion:file:.osparc/jupyter-medimproc/metadata.yml]
search = {current_version}
replace = {new_version}

[bumpversion:file:Makefile]
search = {current_version}
replace = {new_version}

[bumpversion:file:docker-compose-local.yml]
search = {current_version}
replace = {new_version}
